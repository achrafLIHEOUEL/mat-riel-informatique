{% extends 'base.html.twig' %}

{% block title %}Liste des employées{% endblock %}

{% block body %}
    <h1 class="mb-4">👨‍💼 Liste des employées</h1>

    {% if users is empty %}
        <div class="alert alert-warning">Aucun employée trouvé.</div>
    {% else %}
        <table class="table table-striped table-bordered">
            <thead class="table-dark">
                <tr>
                    <th>Nom</th>
                    <th>Prenom</th>
                    <th>Numero de telephone</th>
                    <th>Email</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                {% for user in users %}
                    <tr>
                        <td>{{ user.nom }}</td>
                        <td>{{ user.prenom }}</td>
                        <td>{{ user.numeroTel }}</td>
                        <td>{{ user.email }}</td>
                       
                     
                        <td>
                        <form method="post" action="{{ path('app_supprimer_utilisateur', {'id': user.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet employée ?');">
                          <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
                            <button class="btn btn-danger btn-sm"> Supprimer </button>
                        </form>
                        </td>
    
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endif %}
{% endblock %}
