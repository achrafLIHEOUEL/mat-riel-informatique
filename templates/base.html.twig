<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Accueil{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    {# Lien vers Bootstrap 5 (CDN) #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    {# Ton propre fichier CSS si besoin #}
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">

    {% block stylesheets %}{% endblock %}
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_materiel') }}"> 🖥️ Parc Info</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_acceuil') }}" >Accueil</a>
                    </li>
                    <li class="nav-item dropdown">
                       <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMateriel" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                         Matériel informatique
                       </a>
                       <ul class="dropdown-menu" aria-labelledby="navbarDropdownMateriel">
                           <li><a class="dropdown-item" href="{{ path('app_materiels') }}">Fiche des matériels</a></li>
                           <li><a class="dropdown-item" href="{{ path('app_materiel') }}">Ajouter un matériel</a></li>
                           <li><hr class="dropdown-divider"></li>
        
                      </ul>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_utilisateur') }}"  >Les employées</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('app_profile') }}" class="nav-link" >profile</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" >Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}" >Déconnexion</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>




    <div class="container mt-4">
        {% block body %}{% endblock %}
    </div>

    <footer class="text-center text-muted mt-5 mb-3">
        <hr>
        <p>© 2025 - Parc Info</p>
    </footer>

  
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>

